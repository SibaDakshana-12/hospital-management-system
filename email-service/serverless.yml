service: email-service

frameworkVersion: '3'

provider:
  name: aws
  runtime: python3.11

functions:
  sendEmail:
    handler: handler.send_email_handler
    events:
      - httpApi:
          path: /sendEmail
          method: post

plugins:
  - serverless-offline

custom:
  serverless-offline:
    httpPort: 3000